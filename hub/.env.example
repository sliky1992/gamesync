# Copy to .env and fill in. Used by docker-compose.
STEAMGRIDDB_API_KEY=
# Optional shared secret. If set, the Windows client and web UI must send it.
API_TOKEN=
# Max upload size in bytes (default 512 MB).
MAX_UPLOAD_BYTES=536870912
